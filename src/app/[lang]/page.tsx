import { SecureTimerDashboard } from '@/components/features/timer/SecureTimerDashboard';

/**
 * Renders the secure timer dashboard on the application home route.
 *
 * @returns The home page for the secure timer MVP.
 */
export default function HomePage() {
  return <SecureTimerDashboard />;
}
